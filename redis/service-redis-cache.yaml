apiVersion: v1
kind: Service
metadata:
  name: redis-cache-netbox
  labels:
    app: redis-cache-netbox
  namespace: monitoring  
spec:
  type: ClusterIP
  ports:
    - port: 6379
  selector:
    app: redis-cache-netbox
