apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbox-media
  labels:
    app: netbox
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
